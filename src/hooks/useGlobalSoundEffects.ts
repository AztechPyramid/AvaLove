// Global sound effects hook - disabled to avoid annoying click sounds
export const useGlobalSoundEffects = () => {
  // Click and navigation sounds disabled - users found them intrusive
  // Individual components can still use useSoundEffects() for specific interactions
};
